{
  "name": "ocenka",
  "version": "1.0.0",
  "description": "Ocenka - Интернет-магазин услуг оценки",
  "private": true,
  "scripts": {
    "build": "npm run build:js",
    "build:js": "mkdir -p public/build && terser public/js/navbar.js -o public/build/navbar.min.js --compress --mangle && terser public/js/login.js -o public/build/login.min.js --compress --mangle && terser public/js/admin-orders.js -o public/build/admin-orders.min.js --compress --mangle && terser public/js/order-form.js -o public/build/order-form.min.js --compress --mangle && terser public/js/password-reset-init.js -o public/build/password-reset-init.min.js --compress --mangle && terser public/js/password-reset-confirm.js -o public/build/password-reset-confirm.min.js --compress --mangle",
    "watch": "echo 'Development mode - no minification needed'"
  },
  "devDependencies": {
    "terser": "^5.36.0"
  }
}

